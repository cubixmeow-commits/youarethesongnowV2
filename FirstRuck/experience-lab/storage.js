/* Local-first media and interrupted recording persistence. */
window.RuckStorage=(()=>{let connection;async function db(){if(connection)return connection;connection=new Promise((resolve,reject)=>{const r=indexedDB.open('firstruck-local',1);r.onupgradeneeded=()=>r.result.createObjectStore('items');r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error);});return connection;}async function op(mode,fn){const d=await db();return new Promise((resolve,reject)=>{const tx=d.transaction('items',mode);const request=fn(tx.objectStore('items'));tx.oncomplete=()=>resolve(request.result);tx.onerror=()=>reject(tx.error);tx.onabort=()=>reject(tx.error);});}return {get:key=>op('readonly',s=>s.get(key)),put:(key,value)=>op('readwrite',s=>s.put(value,key)),remove:key=>op('readwrite',s=>s.delete(key))};})();
